[package]
name = "bancaintesa-export"
version = "0.1.0"
edition = "2024"

[dependencies]
calamine = { version = "0.32.0", features = ["chrono"]}
chrono = "0.4.42"
serde = { version = "1.0.228", features = ["derive"] }
